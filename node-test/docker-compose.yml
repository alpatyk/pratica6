version: '3'

services:
  app:
    build: ./app
    ports:
      - "3000:3000"
    networks:
      - minha-rede
  
  tester:
    build: ./tester
    depends_on:
      - app
    networks:
      - minha-rede

networks:
  minha-rede:
    driver: bridge